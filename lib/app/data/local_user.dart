import 'dart:ui';

class LocalUser{
  String? name;
  String? vatNumber;
  String? address;
  String? email;
  String? password;
  Image? logo;

  LocalUser({this.name, this.vatNumber, this.address,
    this.email, this.password, this.logo});
}